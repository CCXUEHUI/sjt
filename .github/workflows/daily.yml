name: Daily Image Crawler

on:
  schedule:
    - cron: "0 18 * * *"   # 每天 UTC 18:00，对应北京时间次日凌晨 2:00
  workflow_dispatch:       # 支持手动触发

jobs:
  run-crawler:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pillow

      - name: Run crawler
        run: |
          python py/crawler.py

      - name: Show files.txt
        run: |
          echo "📄 最新 files.txt 内容："
          cat files.txt || echo "files.txt 不存在"

      - name: Commit and push results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add files.txt
          git commit -m "Update files.txt from crawler" || echo "No changes to commit"
          git push

      - name: Upload files.txt to FTP
        uses: sebastianpopp/ftp-action@v2.0.0
        with:
          host: f27-preview.125mb.com
          port: 21
          user: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          localPath: files.txt
          remotePath: /hehehecao.125mb.com/
